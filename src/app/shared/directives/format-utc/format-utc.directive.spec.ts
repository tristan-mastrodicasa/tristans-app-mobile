import { FormatUtcDirective } from './format-utc.directive';

describe('FormatUtcDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatUtcDirective();
    expect(directive).toBeTruthy();
  });
});
